[[
  @ -> [[
    @ -> $.c.times( 1.8 ).plus( 32.0 ),
    c -> 25.0
  ]],
  bytes -> [[
    @ -> ?
  ]],
  number -> [[
    @ -> ?,
    times -> [[ x -> ?, L> L_number_times ]],
    plus -> [[ x -> ?, L> L_number_plus ]]
  ]]
]]
